using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SoundControllHit : MonoBehaviour
{
    MachineGunManager Manager;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       /* Manager = FindObjectOfType<MachineGunManager>();
        if(Manager.fireloop)
            gameObject.GetComponent<AudioSource>().volume -= Time.deltaTime / 7f;*/

    }
}
